{
  "name": "heliacon-origin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The canonical origin for Heliacon — one source of truth, many projections.",
  "scripts": {
    "build": "tsx build.ts",
    "preview": "tsx build.ts && npx --yes serve dist"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
